"""`vantage` CLI entry point.

Left as a stub — commands are populated in a later prompt (P26). Defined now
so `pip install -e .` registers the `vantage` console script per pyproject.toml.
"""
from __future__ import annotations


def main() -> None:
    raise SystemExit("vantage CLI not yet implemented")
